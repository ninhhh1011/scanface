# Feature completion matrix

Status describes verified scope, not merely the presence of a screen. `IMPLEMENTED_NOT_VERIFIED` means code exists but at least one principal happy/error path still lacks end-to-end evidence. `NEEDS_CONFIGURATION` and `NEEDS_HUMAN_ENROLLMENT` are distinct external gates. All APIs below are authenticated except health/login; authorization is enforced server-side.

Evidence aliases: **HR** = `apps/web/tests/hr-integration.test.ts`; **REG** = `security-regression.test.ts`; **HTTP** = `security-http.test.ts`; **AI** = `ai-*.test.ts` plus `AI_VERIFICATION.md`; **PY** = `PYTHON_VERIFICATION.md`; **BROWSER** = `artifacts/verification/browser-qa.json`; **SLICE** = `artifacts/verification/compatibility-smoke.json`. BROWSER's 27 checks cover four role logins/scopes, four viewports and 15 rendered routes; rendering is not proof of every mutation. Later end-to-end workflow evidence is recorded in `PROGRESS.md`.

Latest consolidated run: `npm run verify -- --http` completed at **2026-09-13 20:47 ICT**, exit2 **PENDING_ACCEPTANCE**. All executed local suites exited0:63 TypeScript tests passed/26 real-provider tests skipped,17 Python tests passed,27 browser QA checks,12 browser flows, runtime slice, lint/typecheck/secrets/doctor. Build passed separately before preview; it was NOT_RUN inside this aggregate. Evidence: `artifacts/verification/verify.json` and named logs. Missing provider, human enrollment and online acceptance are not PASS.

**HTTP** covers53 actual production Workers requests for denial/projection/size/session boundaries and private-file lifecycle. **FLOW** = `artifacts/verification/browser-flows.json`:12 flows, zero page errors, cleanup20 employees. It covers department CRUD, employee create/edit/archive, real shift assignment, contract creation with private attachment upload/download/anonymous denial/delete, reasoned balance create/edit, invalid CSV preview/export/Escape, timesheet20-employee aggregates/CSV, employee leave submit → manager approve → employee cancel with ledger restoration, and consent/camera-denied/unenrolled state. Valid CSV commit is separately verified against actual PostgreSQL in HR tests; human camera success is not implied.

**LIFE** = `apps/web/tests/workflow-lifecycle.test.ts`: five real PostgreSQL lifecycle tests passed as part of the final root suite. Temporary employee/user/2040 business fixtures were cleaned. Shared production services/API handlers receive explicit test actors; session revocation also uses a real persisted hashed session. No biometric/provider success fixtures are involved. Root npm test uses forks with Node `--no-maglev`; this measured Windows native-crash workaround is documented in ARCHITECTURE. Earlier failed runs remain historical evidence, not final PASS.

| Module / workflow | Product route | API / implementation | Policy | Test and evidence | Status / verification boundary |
|---|---|---|---|---|---|
| Login, profile, password change, logout | `/login`, `/profile` | `/api/auth/login`, `me`, `change-password`, `logout` | Current DB session, Origin, cookie flags, revocation | SLICE; BROWSER four roles; HTTP logout/reuse | IMPLEMENTED_AND_VERIFIED for login/current user/logout; password-change browser flow IMPLEMENTED_NOT_VERIFIED |
| Dashboard statistics | `/`, `/dashboard` | `/api/hr/dashboard` | Employee/team/HR scope before aggregates | BROWSER; AI scoped counts | IMPLEMENTED_AND_VERIFIED for scoped database-backed display; exhaustive metric edge cases unverified |
| Employee list/detail, create/edit/archive, CSV | `/employees`, `/employees/:id` | `/api/hr/employees`, `:id`, `:id/archive`, `import`, `template`, `export` | `hr:write` mutations; self/direct-report/HR reads; archive revokes sessions/proofs | HR; REG; BROWSER; FLOW create/edit/archive, invalid CSV and export; HR valid CSV commit and duplicate preview | IMPLEMENTED_AND_VERIFIED for measured lifecycle; valid CSV commit verified through actual API handler/DB |
| Departments and positions | `/departments`, `/positions` | `/api/hr/departments`, `/positions` | Signed-in reads; `hr:write` mutations | SLICE real create/read; HR unauthorized write; FLOW department create/read/update | IMPLEMENTED_AND_VERIFIED for department flow; position mutation browser path unverified |
| Employment contracts and linked files | `/contracts`, employee contract tab | `/api/hr/contracts`, `:id/terminate`; `/api/files` | HR write; salary field needs payroll capability; reason required for termination | REG salary mutation/termination; HTTP salary read; FLOW create/attach/download/deny/delete | IMPLEMENTED_AND_VERIFIED for measured contract/file lifecycle; termination browser interaction unverified |
| Shifts and calendar assignment | `/shifts`, `/schedule` | `/api/hr/shifts`, `/schedule` | HR write; no overlapping assignments or locked-period edits | HR overlap; domain overnight; REG OT overlap; FLOW new employee shift assignment | IMPLEMENTED_AND_VERIFIED for assignment persistence and service overlap rules; all calendar edit browser paths unverified |
| Attendance, manual fallback, correction | `/attendance`, `/attendance/scan` | `/api/hr/attendance`, `manual`, `/corrections/:id/:action` | Self/team reads; reasoned HR manual write; approval scope/no self approval; period locks | HR concurrent/manual check-in/out; REG; HTTP; LIFE correction submit/approve, minutes/audit, locked write denial | IMPLEMENTED_AND_VERIFIED for service lifecycle and authorization; complete correction browser lifecycle unverified |
| Timesheet review/lock/reopen | `/timesheets` | `/api/hr/timesheets/:id/review`, `lock`, `reopen`, scoped detail `items` | HR write, missing checkout rejection; locked payroll prevents reopen | REG; LIFE review→missing-checkout denial→correction→lock, self/other scoped aggregates, payroll lock denial/reopen | IMPLEMENTED_AND_VERIFIED for full service lifecycle; FLOW scoped20-employee detail/CSV verified; browser lock/error cycle unverified |
| Leave requests, approvals, balances | `/leave`, employee leave tab | `/api/hr/leave`, `/leave-balances`, `/leave-types`; submit/approve/reject/cancel | Own requests or HR; direct manager/HR approval, no self approval; assigned weekdays; ledger transaction; reasoned entitlement cannot fall below used | HR; HTTP; FLOW two-account submit/approve/cancel; LIFE balance create/adjust/reason/audit, denied employee mutation and immutable year | IMPLEMENTED_AND_VERIFIED for request/approval/cancellation and entitlement service/browser lifecycle |
| OT request/approval/cancel | `/overtime` | `/api/hr/overtime/:id/:action` | Own request, direct manager/HR approval; overlap/time/period validation | REG overnight; LIFE actual shift change denies both submit and approval, restored schedule allows approve/cancel | IMPLEMENTED_AND_VERIFIED for service lifecycle and changed-schedule negatives; full browser cycle unverified |
| Internal payroll and components | `/payroll`, employee payroll tab | `/api/hr/payroll`, `/payroll-components`; calculate/review/lock/reopen | Payroll mutation capability; locked source; own projected items for non-payroll readers; Copilot own-payroll tool only returns LOCKED periods | REG; HR; HTTP; LIFE calculate/review/lock/reopen, exact own items/money fields, component CSV retains Decimal amounts, contract CSV projection, tool before/after lock | IMPLEMENTED_AND_VERIFIED for service lifecycle/export data; salary-detail CSV button/browser lock cycle unverified; no statutory tax/insurance/payment engine |
| Performance self-review/manager publication | `/performance`, employee assessment tab | `/api/hr/performance`, `/review-cycles`; submit/publish | Own self fields; manager score/comment; unpublished assessment projection | REG; LIFE private draft reads/mutations, forged score denial, premature publish denial, submit/publish and immutable published review | IMPLEMENTED_AND_VERIFIED for service lifecycle; full two-account browser cycle unverified |
| Documents / private storage | `/documents`, employee/contract tabs | `/api/files`, `/api/files/:id`; `/api/hr/documents` | Current owner/object scope; identity capability; HR attachment writes | SLICE actual R2 upload/download/delete and anonymous401; REG unrelated owner; HTTP cross-team | IMPLEMENTED_AND_VERIFIED for local private-file lifecycle; deployed bucket privacy pending |
| Announcements / notifications | `/announcements`, `/notifications` | `/api/hr/announcements/:id/publish`, `archive`; `/notifications/:id/read` | HR authoring; audience roles; per-user notification | LIFE approval generates actual notification; own read and foreign-user denial; BROWSER render | IMPLEMENTED_AND_VERIFIED for notification service read/ownership; announcement browser cycle unverified |
| Reports / CSV exports | `/reports` | `/api/hr/reports?kind=...`, resource `/export` | Same SQL scopes as lists; CSV formula escaping; bounded exports | Domain CSV injection; HR scope and Vietnam-date filter regressions; HTTP denied export data | IMPLEMENTED_NOT_VERIFIED for every report/filter/export browser combination |
| Users, roles and settings | `/settings/users`, `/settings/roles`, `/settings/attendance` | `/api/hr/users`, `/api/hr/roles`, `/api/hr/settings`; lock/unlock/reset-credential | `system:manage`; no self-admin changes; role/password changes revoke sessions | HR; REG; HTTP; LIFE actual isolated user role update removes persisted session and next auth resolves401-equivalent UNAUTHENTICATED | IMPLEMENTED_AND_VERIFIED for role-change/session boundary; full admin/settings lifecycle through UI unverified |
| Audit log | `/audit` | `/api/hr/audit`; shared audit service | System management read; transactional business events; denied access auditing | HR/PY transaction assertions; HTTP admin route denial | IMPLEMENTED_AND_VERIFIED for recorded scoped access/business events; external log retention pending |
| Face enrollment / re-enrollment / deletion | Employee face tab | `/api/face/challenge`, `enroll`, `profiles/:employee`; Python `/face/*` | Consent; current employee/HR authority; signed scope; encrypted templates | PY real models, blank inference, encryption/session/nonce protocols | NEEDS_HUMAN_ENROLLMENT: zero enrolled humans; NV001–NV003 reserved |
| Face recognition → attendance | `/attendance/scan` | `/api/face/verify`, `/attendance`; actual YuNet/SFace service | Unknown/ambiguity/quality/motion thresholds; scoped one-use proof; atomic consumption | PY negative/protocol checks; HTTP forged ID/proof/isLive rejection | NEEDS_HUMAN_ENROLLMENT: real new-capture recognition, accuracy, unknown and liveness remain unverified |
| Knowledge upload / jobs / version lifecycle | `/knowledge-base` | `/api/knowledge`, version/reindex/publish/deactivate/delete; durable Python worker | Knowledge capability; metadata ACL/effective dates; private files; least privilege DB | Ten actual uploaded policies, ten FAILED jobs after three missing-provider attempts; AI DB lifecycle | NEEDS_CONFIGURATION: zero chunks, no indexed/published evidence claimed |
| Structured HR Copilot tools | `/assistant` | `/api/assistant`, `brief`; eleven read-only tools | Strict allowlist, SQL/object/field scope; bounded arguments; current history fingerprint | AI actual PostgreSQL tools/scopes/aggregate checks; missing-provider HTTP503 | IMPLEMENTED_AND_VERIFIED for tool services; actual model routing NEEDS_CONFIGURATION |
| Policy RAG / mixed answers / citations / history | `/assistant` | `/api/assistant/sessions`, messages; `/api/knowledge/chunks/:id` | ACL/effective/active version filters on both retrieval branches and source open; output guards before response | AI schema/fake-provider and DB scope tests; 30-case evaluation dataset; real RAG gate explicitly skipped | NEEDS_CONFIGURATION: real embedding, retrieval, mixed answer/citations and model attack evaluation not verified |
| HeroUI theme / responsive application | All above routes | Real HeroUI v3 + Inter Tight; shared shell/forms/dialogs | Navigation follows capabilities; backend remains authority | BROWSER four viewports, zero page errors; UI component tests | IMPLEMENTED_AND_VERIFIED for measured screens/viewports; exhaustive keyboard/device coverage pending |
| Workers/PostgreSQL/R2/Python compatibility | Authenticated normal application | vinext production Workers build, Prisma edge adapter, R2, outbound Python | No public diagnostic secrets; per-request DB lifecycle | SLICE ten checks incl real persistence/vector/storage/Python ready | IMPLEMENTED_AND_VERIFIED locally; online deployment NEEDS_CONFIGURATION |
| Deployment, HTTPS camera and restart durability | Production URLs pending | Cloudflare Workers/R2 + PostgreSQL pgvector + separate AI/worker container | Managed secrets, private service/storage, restricted role | Local Docker/model/Workers evidence only | NEEDS_CONFIGURATION / DEPLOYMENT_PENDING; no online URL or redeploy persistence claim |

Seed has exactly NV001–NV020 and marked synthetic business history, never synthetic identities. Model fixtures are classified in PY; a generated blank image proves inference/rejection plumbing only. No online credentials or model-provider keys have been supplied, and no public resources, commits or pushes are created by this implementation.
