# ABC HRM

The complete master prompt is authoritative scope. Vietnamese HR modular monolith, real face attendance and evidence-grounded read-only HR Copilot. Four roles: SUPER_ADMIN, HR_ADMIN, MANAGER (direct reports), EMPLOYEE (self). Separate payroll and sensitive document capabilities.

Delivery order: compatibility → employee/org/contracts → shifts/attendance/leave/OT/timesheet/payroll/reviews/files/notices → face → knowledge/Copilot → security/UX → online verification. Existing routes remain real product workflows, never a presentation mode.

Acceptance includes happy paths, denied/error paths, real DB transactions, restart persistence, exact idempotent 20-record seed, zero seeded faces, versioned/ACL-filtered real embeddings, no false PASS. Online/provider/human gates remain distinct from code completion.
